import Skeleton from '@/components/Skeleton';
import ContentLoader, { Rect } from 'react-content-loader/native';

export default function SkeletonCard({}) {
	return <Skeleton height={80} borderRadius={16} />;
}
